{
  "name": "astro-golden-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.6",
    "@astrojs/rss": "^4.0.18",
    "astro": "^6.1.2"
  },
  "devDependencies": {
    "wrangler": "^4.79.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
